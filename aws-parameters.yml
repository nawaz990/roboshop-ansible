- name: Create All RoboShop Parameters
  hosts: localhost
  tasks:
    - name: Create Value
      community.aws.aws_ssm_parameter_store:
        name: "{{item.name}}"
        value: "{{item.value}}"
        region: "us-east-1"
      loop:
        - {name: "param.dev.catalogue.MONGO", value: "true"}
        - {name: "param.dev.catalogue.MONGO_URL", value: "'mongodb://172.31.1.70:27017/catalogue'"}
        - {name: "param.dev.user.MONGO", value: "true"}
        -

